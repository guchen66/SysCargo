<UserControl x:Class="仓库管理系统.Shell.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:仓库管理系统.Shell.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Background="AliceBlue">
        <!--<StackPanel >
            <TextBox Width="200" Height="40" Text="{Binding Id}"/>
            <TextBox Width="200" Height="40" Text="{Binding Name}" Margin="4"/>
            <TextBox Width="200" Height="40" Text="{Binding Price}"/>
            
        </StackPanel>-->
       
    </Grid>
</UserControl>
